bin_PROGRAMS = first
first_SOURCES = first.c events.c process.c virkb.c header.h
first_LDADD = msk0/libmsk0.la mod-winmm/modwinmm.la $(GTK_LIBS)
first_CFLAGS = $(GTK_CFLAGS)

SUBDIRS = msk0 mod-winmm
